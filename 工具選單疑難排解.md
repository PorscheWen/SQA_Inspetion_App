# 工具選單疑難排解

## 問題：公司電腦工具選單.bat 無法執行或 crash

### 📋 症狀

- 雙擊執行後視窗一閃即逝
- 顯示亂碼或特殊字符無法正常顯示
- 選擇選項後程式崩潰

---

## ✅ 解決方法

### 方法 1：使用簡化版選單（推薦）

如果原版選單無法正常運作，請使用簡化版：

```cmd
雙擊： 工具選單（簡化版）.bat
```

**簡化版特點：**
- ✅ 不使用特殊 Unicode 字符（避免編碼問題）
- ✅ 簡潔的文字界面
- ✅ 相同的功能
- ✅ 更好的相容性

---

### 方法 2：從命令提示字元執行（查看錯誤）

1. 按 `Win + R`
2. 輸入 `cmd` 並按 Enter
3. 使用 `cd` 切換到專案目錄：
   ```cmd
   cd C:\path\to\SQA_Inspetion_App
   ```
4. 執行選單：
   ```cmd
   公司電腦工具選單.bat
   ```
5. 查看完整的錯誤訊息

---

### 方法 3：檢查編碼設定

確認命令提示字元使用 UTF-8 編碼：

```cmd
chcp 65001
```

然後重新執行選單。

---

### 方法 4：直接執行想要的功能

如果選單無法使用，可以直接執行對應的批次檔：

| 功能 | 批次檔 |
|------|--------|
| 操作手冊 | `開啟操作手冊（無Python）.bat` |
| 執行測試 | `run_tests.bat` |
| 單一測試 | `執行單一測試.bat` |
| 測試報告 | `開啟測試報告.bat` |
| 啟動程式 | `啟動InspectionApp.bat` |

---

## 🔍 常見原因

### 1. 編碼問題

**症狀：** 特殊字符顯示為亂碼或方框

**原因：** 
- 命令提示字元未使用 UTF-8 編碼
- Windows 系統語言設定問題

**解決：**
- 使用 `工具選單（簡化版）.bat`（不使用特殊字符）
- 在命令提示字元執行 `chcp 65001`

---

### 2. 路徑問題

**症狀：** 顯示「找不到檔案」錯誤

**原因：**
- 批次檔不在專案根目錄執行
- 專案路徑包含特殊字符

**解決：**
- 確保批次檔在專案根目錄（與 README.md 同一層）
- 避免專案路徑包含空格或中文

---

### 3. 權限問題

**症狀：** 選擇選項後無反應

**原因：**
- 沒有執行批次檔的權限
- 公司電腦有群組原則限制

**解決：**
- 右鍵批次檔 → 「以系統管理員身分執行」
- 聯繫 IT 部門確認是否有限制

---

### 4. 缺少檔案

**症狀：** 執行特定選項時顯示「找不到檔案」

**原因：**
- 專案檔案不完整
- Git clone 或下載時出現問題

**解決：**
```cmd
# 重新從 GitHub 下載
git clone https://github.com/PorscheWen/SQA_Inspetion_App.git

# 或檢查專案完整性
dir *.bat
```

確認以下檔案存在：
- `開啟操作手冊（無Python）.bat`
- `run_tests.bat`
- `執行單一測試.bat`
- `開啟測試報告.bat`
- `啟動InspectionApp.bat`

---

## 🎯 兩個版本的差異

### 公司電腦工具選單.bat（原版）

**優點：**
- ✅ 美觀的界面（使用 Unicode 繪圖字符）
- ✅ 視覺化的分隔線
- ✅ emoji 圖示

**缺點：**
- ⚠️ 某些 Windows 環境可能顯示亂碼
- ⚠️ 需要 UTF-8 編碼支援

**適用：** Windows 10/11 with UTF-8 support

---

### 工具選單（簡化版）.bat（新版）

**優點：**
- ✅ 純 ASCII 字符
- ✅ 更好的相容性
- ✅ 不會有編碼問題
- ✅ 功能完全相同

**缺點：**
- ⚠️ 界面較簡樸

**適用：** 所有 Windows 版本，尤其是公司電腦

---

## 💡 建議

### 對於公司電腦用戶

1. **優先使用簡化版：**
   ```cmd
   工具選單（簡化版）.bat
   ```

2. **如果簡化版也無法使用：**
   - 直接執行對應的批次檔
   - 參考「公司電腦快速參考.md」

3. **如果所有批次檔都無法使用：**
   - 可能是公司有群組原則限制
   - 聯繫 IT 部門
   - 或使用命令列手動執行：
     ```cmd
     dotnet test -c Release
     ```

---

## 📋 測試清單

驗證選單是否正常工作：

- [ ] 可以顯示選單（無亂碼）
- [ ] 輸入數字後正確執行對應功能
- [ ] 選擇 0 可以正常退出
- [ ] 輸入無效數字會顯示錯誤並返回選單
- [ ] 執行完功能後會暫停等待按鍵

---

## 🆘 還是無法解決？

### 最簡單的方式：不使用選單

直接雙擊您需要的批次檔：

```
專案根目錄/
├── 開啟操作手冊（無Python）.bat    ← 查看教學
├── run_tests.bat                    ← 執行測試
├── 開啟測試報告.bat                 ← 查看報告
├── 啟動InspectionApp.bat           ← 啟動程式
└── ...
```

**選單只是為了方便**，不使用選單也能完成所有工作！

---

## 📞 聯絡支援

如果問題持續存在，請提供以下資訊：

1. Windows 版本：
   ```cmd
   ver
   ```

2. 命令提示字元編碼：
   ```cmd
   chcp
   ```

3. 錯誤訊息（從命令提示字元執行的完整輸出）

4. 使用哪個版本的選單
   - 公司電腦工具選單.bat
   - 工具選單（簡化版）.bat

---

**總結：如果原版選單有問題，請使用「工具選單（簡化版）.bat」或直接執行對應的批次檔。**
